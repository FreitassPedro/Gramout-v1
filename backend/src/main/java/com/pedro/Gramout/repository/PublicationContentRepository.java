package com.pedro.Gramout.repository;

import com.pedro.Gramout.entity.PublicationContent;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PublicationContentRepository extends JpaRepository<PublicationContent, Long> {
}
